node_modules
client/node_modules/
client/build/

# Prevent deployment of duplicate content directories
website/
_client-disabled/
_server-backup/
deployment-archive/
android/
ios/

# Legacy documentation and scripts
docs/legacy/
scripts/legacy/
config/legacy/
test-files/

# Static files that conflict with React build
index-static-backup.html

# Test and admin files
test*.html
admin*.html

# Build artifacts and temp files
*.log
*.tmp
.cache
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# React build artifacts in root - keep needed files, exclude large maps and old builds
static/js/*.map
static/js/*.LICENSE.txt
# Keep only the latest asset-manifest.json for deployment
# Exclude old JS files except the most recent one

# IDE files
.vscode/
.idea/
*.swp
*.swo
build/
