# Suppress deprecation warnings during install
# These warnings come from react-scripts 5.0.1 transitive dependencies
# and are safe to ignore for production builds
legacy-peer-deps=true
audit-level=moderate
fund=false
# Suppress npm warnings for deprecated packages that can't be updated
# without breaking changes to react-scripts
silent=true