{
  "buildCommand": "cd client && npm ci --no-fund --no-audit && npm run build",
  "outputDirectory": "client/build",
  "installCommand": "npm install --no-fund --no-audit",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}