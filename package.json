{
  "name": "fixloapp",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "install-client": "cd client && npm install",
    "build-client": "cd client && REACT_APP_API_URL=https://fixloapp.onrender.com npm run build",
    "start-client": "cd client && npm start",
    "build": "npm run install-client && npm run build-client"
  }
}
