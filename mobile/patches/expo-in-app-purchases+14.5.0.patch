diff --git a/app.plugin.js b/app.plugin.js
deleted file mode 100644
index 0000000..0000000
--- a/app.plugin.js
+++ /dev/null
@@ -1,10 +0,0 @@
-// dummy deleted plugin file removed to prevent EAS Build errors
-// Original expo-in-app-purchases app.plugin.js caused “Unexpected token 'typeof'”
-
-module.exports = function withNoopPlugin(config) {
-  console.log("🧩 Skipping invalid expo-in-app-purchases plugin.");
-  return config;
-};
-
-// This ensures EAS Build can read config without crashing while runtime StoreKit stays intact.
-
